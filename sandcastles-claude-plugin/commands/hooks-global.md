---
description: See top hook patterns across all of Sandcastles
---
Show me the top hook patterns working across all of Sandcastles, beyond my watchlist. Use the `top_hooks` tool with `scope: "all"` to retrieve high-performing analyzed videos globally, then synthesize across `spoken_hook_category` and `spoken_hook_madlib` to identify the patterns that are working broadly. Cite 2-3 example videos per pattern with their Sandcastles links.
