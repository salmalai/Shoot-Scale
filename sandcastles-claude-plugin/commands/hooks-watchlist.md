---
description: See top hook patterns from creators in your watchlist
---
Show me the top hook patterns working among the channels I follow. Use the `top_hooks` tool with `scope: "watchlist"` to retrieve analyzed videos from my watchlist, then synthesize across `spoken_hook_category` and `spoken_hook_madlib` to identify the patterns that are working. Cite 2-3 example videos per pattern with their Sandcastles links.
